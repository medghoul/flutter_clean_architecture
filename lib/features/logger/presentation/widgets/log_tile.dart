import 'package:flutter/material.dart';

class LoggerWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('This is a logger widget'),
    );
  }
}